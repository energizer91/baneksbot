// @flow
import type Queue from '../queue';

declare type RequestConfig = {
  url: string,
  method: 'get' | 'post'
}

declare type RequestParams = {
  _key: string,
  _rule: string,

}

declare var queue: Queue;

declare export default class NetworkModel {
  makeRequest(config: RequestConfig, params: RequestParams): Promise<any>;
  fulfillAll<T>(requests: Promise<T>[]): Promise<T[]>;
  fulfillAllSequentally<T>(requests: Promise<T>[]): Promise<T[]>;
}
